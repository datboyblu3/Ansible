-
	name: 'Install security tools'
	hosts: all
	become: yes
	vars:
		packages:
		- curl
		- synaptic
		- timeshift
    	- simplescreenrecorder
		- suricata
		- zeek
	
	tasks:
		- 
			apt:
				name: '{{ item }}'
				state: present
				update_cache: yes
			with_item: '{{ packages }}'
				
