-
    name: 'Install security tools on Debian systems'
    hosts: Ubuntu
    become: true
    vars:
        packages:
        - zeek
        - suricata
        - vim
        
    tasks:
        -
          apt:
              name: '{{ item }}'
              state: present
              update_cache: yes
          when_items: '{{ packages }}'
          when: ansible_os_family == 'Debian'
